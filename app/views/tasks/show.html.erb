<h2><%= @task.name %></h2>
<p><%= @task.done %></p>


<%= link_to "Deletar",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Tem certeza que deseja deletar a tarefa #{@task.name}?"}
%>

<%= link_to "Editar",  edit_task_path(@task) %>
